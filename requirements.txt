# Core
streamlit>=1.33.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
websocket-client>=1.6.0
python-dotenv>=1.0.0
yfinance>=1.2.2,<1.3.0
curl_cffi>=0.15
lxml>=5.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
requests-mock>=1.11.0

# Optional — ML predicted price profile (agent_stonks/profile_model.py).
# Loads the LevelsML density model pack from ../Models; the chart overlay
# silently stays off without this dependency or the pack file.
lightgbm>=4.0.0

# Optional — the pickled scikit-learn parts of the saved models: the day-range
# bundle behind Apple Trader (agent_stonks/dayrange_model.py) and the
# news-impact model. Unpickling needs the same major version they were fitted
# with; without these the models report themselves unavailable.
scikit-learn>=1.9.0
joblib>=1.4.0

# Optional — PyTorch, for the TimeToChange3 day-range model
# (agent_stonks/dayrange_model.py), which is the one thing here that needs it
# and LightGBM at once: its blend is one LightGBM pair and two torch networks.
#
# NOTE: torch and lightgbm each bundle their own OpenMP runtime and this
# environment has no system libomp for them to share, and the conflict cuts
# both ways. A process that imports torch first segfaults when it later loads
# a LightGBM model, so dayrange_model.py imports lightgbm ahead of torch. That ordering then arms the opposite trap -- with LightGBM's
# runtime loaded first, the next torch op that goes parallel takes the process
# down (a MaxPool1d on a tiny tensor is enough) -- so dayrange_model.py also
# calls torch.set_num_threads(1) at import, which works whatever the order was.
torch>=2.6

# Optional — LLM pipeline (agent_stonks/llm.py, news.py, agent.py)
openai>=1.0.0
anthropic>=0.34.0
worldnewsapi>=2.0.0
pydantic>=2.0.0

# Optional — LLM observability (agent_stonks/observability.py). No-op unless
# LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY (and optionally LANGFUSE_HOST) are set.
langfuse>=3.0.0
