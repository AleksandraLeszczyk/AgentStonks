# Core
streamlit>=1.33.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
websocket-client>=1.6.0
python-dotenv>=1.0.0
yfinance>=0.2.40,<1.3.0   # 1.3+ requires curl_cffi>=0.15 which needs Python>=3.10
curl_cffi>=0.14.0b2,<0.15  # 0.14+ has TLS fingerprint required by Yahoo Finance; 0.15 needs Python>=3.10
lxml>=5.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
requests-mock>=1.11.0

# Optional — LLM pipeline (marketview/llm.py, news.py, agent.py)
openai>=1.0.0
anthropic>=0.34.0
worldnewsapi>=2.0.0
pydantic>=2.0.0

# Optional — LLM observability (marketview/observability.py). No-op unless
# LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY (and optionally LANGFUSE_HOST) are set.
langfuse>=3.0.0
